[build-system]
# Minimum requirements for the build system to execute.
requires = [
        "setuptools>=42.0.0",
        "wheel>=0.34.2",
        "pybind11>=2.5.0"
        ]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]

skip = "*-win-* [cp]p3[67]-*"
